extends layouts/layout

block content
  h1 Welcome to the Dance Class Website!
  img(src='/images/logo.png', alt='Dance Class Logo')
  include ./partials/classes
  include ./partials/instructors
  include ./partials/testimonials
  include ./partials/gallery
  
  .container
    h1 Our Location
    #map(style='height: 500px;')
    script
      | function initMap() {
      |   var map = new google.maps.Map(document.getElementById('map'), {
      |     center: { lat: -34.397, lng: 150.644 },
      |     zoom: 8
      |   });
      | }
    script(src='https://maps.googleapis.com/maps/api/js?key=YOUR_GOOGLE_MAPS_API_KEY&callback=initMap', async, defer)